<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.honker</groupId>
  <artifactId>honker</artifactId>
  <version>0.5.0</version>
  <name>honker-jvm</name>
  <description>JVM binding for Honker queues, streams, pub/sub, scheduler, locks, and rate limits on SQLite.</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <sqlite-jdbc.version>3.53.2.1</sqlite-jdbc.version>
    <junit.version>6.1.1</junit.version>
    <honker.native.platform>linux-x64</honker.native.platform>
    <honker.native.name>libhonker_ext.so</honker.native.name>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>${sqlite-jdbc.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>1.0.0</version>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>3.2.0</version>
        <executions>
          <execution>
            <id>copy-native-extension</id>
            <phase>process-resources</phase>
            <configuration>
              <!--
                Packages the extension the jar ships to users, which is the
                only way it is found once the jar leaves this repo (see
                scripts/test-jvm-consumer.sh, which unsets
                HONKER_EXTENSION_PATH). This used to copy `target/debug`
                only, with failonerror="false", so a release cargo build
                followed by `mvn install` gave a green build and a jar whose
                runtimes/ directory was empty.

                Take whichever of debug and release was built last, so a
                stale artifact never beats the one you just compiled, and
                fail the build when there is neither.
              -->
              <target>
                <property name="honker.native.release"
                          location="${project.basedir}/../../target/release/${honker.native.name}"/>
                <property name="honker.native.debug"
                          location="${project.basedir}/../../target/debug/${honker.native.name}"/>
                <condition property="honker.native.src"
                           value="${honker.native.debug}"
                           else="${honker.native.release}">
                  <and>
                    <available file="${honker.native.debug}"/>
                    <or>
                      <not><available file="${honker.native.release}"/></not>
                      <uptodate srcfile="${honker.native.release}"
                                targetfile="${honker.native.debug}"/>
                    </or>
                  </and>
                </condition>
                <fail message="Honker extension not built for ${honker.native.platform}. Looked for ${honker.native.release} and ${honker.native.debug}. Run: cargo build --release -p honker-extension">
                  <condition>
                    <not><available file="${honker.native.src}"/></not>
                  </condition>
                </fail>
                <mkdir dir="${project.build.outputDirectory}/runtimes/${honker.native.platform}/native"/>
                <copy file="${honker.native.src}"
                      todir="${project.build.outputDirectory}/runtimes/${honker.native.platform}/native"/>
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.6</version>
        <configuration>
          <useModulePath>false</useModulePath>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native-osx-arm64-aarch64</id>
      <activation>
        <os>
          <family>mac</family>
          <arch>aarch64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>osx-arm64</honker.native.platform>
        <honker.native.name>libhonker_ext.dylib</honker.native.name>
      </properties>
    </profile>
    <profile>
      <id>native-osx-arm64</id>
      <activation>
        <os>
          <family>mac</family>
          <arch>arm64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>osx-arm64</honker.native.platform>
        <honker.native.name>libhonker_ext.dylib</honker.native.name>
      </properties>
    </profile>
    <profile>
      <id>native-osx-x64</id>
      <activation>
        <os>
          <family>mac</family>
          <arch>x86_64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>osx-x64</honker.native.platform>
        <honker.native.name>libhonker_ext.dylib</honker.native.name>
      </properties>
    </profile>
    <profile>
      <id>native-linux-arm64</id>
      <activation>
        <os>
          <name>Linux</name>
          <arch>aarch64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>linux-arm64</honker.native.platform>
        <honker.native.name>libhonker_ext.so</honker.native.name>
      </properties>
    </profile>
    <profile>
      <id>native-linux-x64</id>
      <activation>
        <os>
          <name>Linux</name>
          <arch>amd64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>linux-x64</honker.native.platform>
        <honker.native.name>libhonker_ext.so</honker.native.name>
      </properties>
    </profile>
    <profile>
      <id>native-windows-x64</id>
      <activation>
        <os>
          <family>windows</family>
          <arch>amd64</arch>
        </os>
      </activation>
      <properties>
        <honker.native.platform>win-x64</honker.native.platform>
        <honker.native.name>honker_ext.dll</honker.native.name>
      </properties>
    </profile>
  </profiles>
</project>
